#include "langtraining.h"
#include <vector>
#include <string>

using namespace std;

map<string trigram, int frequency> trigrams(string input)
{
    for (int i = 0; i < input.length() - 3; i+=3)
    {
        
    }
}
