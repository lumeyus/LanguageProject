#include "langtraining.h"
#include <vector>
#include <string>
#include <map>

using namespace std;

map<string, int> trigrams(string input)
{
    for (int i = 0; i < input.length() - 3; i+=3)
    {

    }
}
